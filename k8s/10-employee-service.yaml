apiVersion: apps/v1
kind: Deployment
metadata:
  name: employee-service
  namespace: hrms
spec:
  replicas: 2   # baseline replicas — HPA isko traffic ke hisaab se badhayega
  selector: { matchLabels: { app: employee-service } }
  template:
    metadata: { labels: { app: employee-service } }
    spec:
      containers:
        - name: employee-service
          image: your-registry/employee-service:latest   # docker build+push karke yahan apna image daalo
          ports: [{ containerPort: 5002 }]
          envFrom:
            - configMapRef: { name: hrms-config }
          env:
            - name: PORT
              value: "5002"
            - name: MONGO_URI
              valueFrom: { configMapKeyRef: { name: hrms-config, key: MONGO_URI_EMPLOYEE } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: hrms-secrets, key: JWT_SECRET } }
            - name: JWT_REFRESH_SECRET
              valueFrom: { secretKeyRef: { name: hrms-secrets, key: JWT_REFRESH_SECRET } }

          resources:
            requests: { cpu: "150m", memory: "150Mi" }
            limits: { cpu: "500m", memory: "400Mi" }
          readinessProbe:
            httpGet: { path: /health, port: 5002 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 5002 }
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: employee-service
  namespace: hrms
spec:
  selector: { app: employee-service }
  ports: [{ port: 5002, targetPort: 5002 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: employee-service-hpa
  namespace: hrms
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: employee-service
  minReplicas: 2
  maxReplicas: 15
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 65 }
    - type: Resource
      resource:
        name: memory
        target: { type: Utilization, averageUtilization: 75 }
