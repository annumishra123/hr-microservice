apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: hrms
spec:
  replicas: 2   # baseline replicas — HPA isko traffic ke hisaab se badhayega
  selector: { matchLabels: { app: auth-service } }
  template:
    metadata: { labels: { app: auth-service } }
    spec:
      containers:
        - name: auth-service
          image: your-registry/auth-service:latest   # docker build+push karke yahan apna image daalo
          ports: [{ containerPort: 5001 }]
          envFrom:
            - configMapRef: { name: hrms-config }
          env:
            - name: PORT
              value: "5001"
            - name: MONGO_URI
              valueFrom: { configMapKeyRef: { name: hrms-config, key: MONGO_URI_AUTH } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: hrms-secrets, key: JWT_SECRET } }
            - name: JWT_REFRESH_SECRET
              valueFrom: { secretKeyRef: { name: hrms-secrets, key: JWT_REFRESH_SECRET } }

          resources:
            requests: { cpu: "150m", memory: "150Mi" }
            limits: { cpu: "500m", memory: "400Mi" }
          readinessProbe:
            httpGet: { path: /health, port: 5001 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 5001 }
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: hrms
spec:
  selector: { app: auth-service }
  ports: [{ port: 5001, targetPort: 5001 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auth-service-hpa
  namespace: hrms
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: auth-service
  minReplicas: 2
  maxReplicas: 15
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 65 }
    - type: Resource
      resource:
        name: memory
        target: { type: Utilization, averageUtilization: 75 }
