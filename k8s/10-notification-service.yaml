apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: hrms
spec:
  replicas: 2   # baseline replicas — HPA isko traffic ke hisaab se badhayega
  selector: { matchLabels: { app: notification-service } }
  template:
    metadata: { labels: { app: notification-service } }
    spec:
      containers:
        - name: notification-service
          image: your-registry/notification-service:latest   # docker build+push karke yahan apna image daalo
          ports: [{ containerPort: 5004 }]
          envFrom:
            - configMapRef: { name: hrms-config }
          env:
            - name: PORT
              value: "5004"
            - name: MONGO_URI
              valueFrom: { configMapKeyRef: { name: hrms-config, key: MONGO_URI_NOTIFICATION } }
            - name: JWT_SECRET
              valueFrom: { secretKeyRef: { name: hrms-secrets, key: JWT_SECRET } }
            - name: JWT_REFRESH_SECRET
              valueFrom: { secretKeyRef: { name: hrms-secrets, key: JWT_REFRESH_SECRET } }

          resources:
            requests: { cpu: "150m", memory: "150Mi" }
            limits: { cpu: "500m", memory: "400Mi" }
          readinessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /health, port: 5004 }
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: hrms
spec:
  selector: { app: notification-service }
  ports: [{ port: 5004, targetPort: 5004 }]
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: notification-service-hpa
  namespace: hrms
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: notification-service
  minReplicas: 2
  maxReplicas: 15
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 65 }
    - type: Resource
      resource:
        name: memory
        target: { type: Utilization, averageUtilization: 75 }
